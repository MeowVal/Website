---
deployment:
  tasks:
    - export DEPLOYPATH=/home/catkatny/public_html/
    - export PATH=$HOME/bin:$PATH
    - cd $DEPLOYMENT_SOURCE
    - npm install
    - nom run build
    - rsync -av dist/ $DEPLOYPATH