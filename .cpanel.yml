---
deployment:
  tasks:
    - export DEPLOYPATH=/home/catkatny/public_html/
    - export PATH=$HOME/nodevenv/nodeapp/22/bin:$PATH
    - cd $DEPLOYMENT_SOURCE/website
    - npm install
    - nom run build
    - rsync -av dist/ $DEPLOYPATH